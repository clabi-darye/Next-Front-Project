import MicIcon from "@mui/icons-material/Mic";

export const SearchBoxConfig = {
  isVoiceSearch: true,
  micIcon: (
    <MicIcon
      fontSize="large"
      sx={{
        color: "var(--point)",
      }}
    />
  ),
};
